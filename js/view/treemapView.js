var TreemapView = function(container,model,day)
{
	// Load the treemap here in the "container" with the data from "day".
	// Possibly you need to convert the data in day to a JSON format that
	// The d3 treemap expects. I recommend http://bl.ocks.org/mbostock/4063582
	// Because we don't need to zoom.
}